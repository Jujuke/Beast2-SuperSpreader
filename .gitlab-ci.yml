stages:
  - build

build:
  stage: build
  image: debian:stable
  variables:
    JAVA_FX_HOME: /usr/share/java/
  before_script:
    - apt-get update
    - apt-get install -y ant openjdk-21-jdk openjfx ant unzip wget
  script:
    - ant